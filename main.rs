use std::io;

struct Lexer<'a> {
    program: &'a str,
}
struct Parser<'a> {
    prog: &'a str,
}

impl Lexer<'_> {
    pub fn tokenize() {todo!()}
    pub fn evaluate() {todo!()}
}
impl Parser<'_> {
    pub fn codegen() {todo!()}
    pub fn parse() {todo!()}
}

fn main() {
    // return Parser;
    // return Lexer;
    todo!();
}
